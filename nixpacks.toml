# Force Railway to use Node.js instead of auto-detecting Deno from supabase functions
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm start"
